#!/bin/zsh


_cdd () {
	ARR_OPTS=() 
	TARGET2GO='.' 
	for param in ${@}
	do
		startsWithDash "${param}" && ARR_OPTS+="${param}"  || TARGET2GO="${param}" 
	done
	[[ -d "${TARGET2GO}" ]] || TARGET2GO=$(dirname "${TARGET2GO}") 
	builtin cd ${ARR_OPTS} ${TARGET2GO}
}
_cdd ${@}
